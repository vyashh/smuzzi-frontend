import { Button, View } from "react-native";
import AppText from "./AppText";
import App from "App";
import { globalStyles } from "constants/global";

const CreatePlaylist = () => {
  return (
    <View>
      <AppText>Create Playlist</AppText>
    </View>
  );
};

export default CreatePlaylist;
