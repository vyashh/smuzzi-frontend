import Ionicons from "@expo/vector-icons/Ionicons";
import { Colors } from "../constants/colors";

const LikeButton = () => {
  return <Ionicons name="heart-outline" size={32} color={Colors.primary} />;
};

export default LikeButton;
